<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>MergeToMasterRecordId__c</fullName>
    <description>Formula field to determine which Account should be set as the master when the record is merged</description>
    <externalId>false</externalId>
    <formula>LEFT(Id,15) = LEFT(MergeToRecordId__c,15)</formula>
    <label>Merge to Master Record ID</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
